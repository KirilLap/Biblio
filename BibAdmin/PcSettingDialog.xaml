<Window x:Class="BibAdmin.PcSettingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка ПК"
        SizeToContent="Height" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTitle" Grid.Row="0"
                   Foreground="White" FontSize="16"
                   FontWeight="Bold" HorizontalAlignment="Center"
                   Margin="0,0,0,4"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,0,0,16">
            <TextBlock Text="ПК №" Foreground="#666688" FontSize="12"/>
            <TextBlock x:Name="TxtPcNumber" Foreground="#AAAACC" 
                       FontSize="12" FontWeight="Bold"/>
        </StackPanel>

        <TextBlock x:Name="TxtLabel" Grid.Row="2"
                   Foreground="#AAAACC" FontSize="12"
                   Margin="0,0,0,6"/>

        <Border Grid.Row="3" BorderBrush="#3D3D6B"
                BorderThickness="1" CornerRadius="8"
                VerticalAlignment="Top">
            <TextBox x:Name="TxtInput"
                     Background="#2A2A3E" Foreground="White"
                     BorderThickness="0" FontSize="14"
                     Padding="12,10" CaretBrush="White"/>
        </Border>

        <Button Grid.Row="4" Content="Применить"
                Click="BtnApply_Click"
                Background="#3D3D6B" Foreground="White"
                FontSize="14" Padding="0,12"
                BorderThickness="0" Cursor="Hand"
                Margin="0,16,0,0">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>