<Window x:Class="BibAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BibAdmin — Панель управления"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAACC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,11"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2E2E4E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter TargetName="border" Property="Background" Value="#3D3D6B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковое меню -->
        <Border Grid.Column="0" Background="#1E1E2E">
            <DockPanel>

                <!-- Логотип -->
                <Border DockPanel.Dock="Top" Padding="20,24,20,18"
                        BorderBrush="#2E2E4E" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="BibAdmin"
                                   Foreground="White" FontSize="18"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Панель управления"
                                   Foreground="#666688" FontSize="11"
                                   Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Кнопка выхода -->
                <Border DockPanel.Dock="Bottom" Padding="12,10"
                        BorderBrush="#2E2E4E" BorderThickness="0,1,0,0">
                    <Button Content="✕  Выход"
                            Padding="14,9" FontSize="13" Cursor="Hand"
                            HorizontalContentAlignment="Left"
                            BorderThickness="0"
                            Background="Transparent"
                            Foreground="#664455"
                            Click="BtnExit_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#3A1520"/>
                                        <Setter Property="Foreground" Value="#E24B4A"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Border>

                <!-- Статус сервера -->
                <Border DockPanel.Dock="Bottom" Padding="20,14"
                        BorderBrush="#2E2E4E" BorderThickness="0,1,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="DotServer" Width="8" Height="8"
                                     Fill="#E24B4A" VerticalAlignment="Center"
                                     Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtServerStatus"
                                       Text="Запуск сервера..."
                                       Foreground="#666688" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Навигация -->
                <StackPanel Margin="12,16,12,0">
                    <Button x:Name="BtnComputers"
                            Content="🖥  Компьютеры"
                            Style="{StaticResource NavBtn}"
                            Tag="active"
                            Click="BtnComputers_Click"/>
                    <Button x:Name="BtnFinance"
                            Content="💰  Финансы"
                            Style="{StaticResource NavBtn}"
                            Click="BtnFinance_Click"/>
                    <Button x:Name="BtnSettings"
                            Content="⚙  Настройки"
                            Style="{StaticResource NavBtn}"
                            Click="BtnSettings_Click"/>

                    <!-- Разделитель -->
                    <Border Height="1" Background="#2E2E4E" Margin="4,12"/>

                    <!-- Кнопка быстрой услуги -->
                    <Button x:Name="BtnNewService"
                            Content="🛎  Оказать услугу"
                            Padding="14,11" Margin="0,2"
                            FontSize="13" Cursor="Hand"
                            HorizontalContentAlignment="Left"
                            BorderThickness="1"
                            BorderBrush="#2A7A5A"
                            Background="#1D3D30"
                            Foreground="#4DC79A"
                            Click="BtnNewService_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1D9E75"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

            </DockPanel>
        </Border>

        <!-- Основная область -->
        <Frame x:Name="MainFrame"
               Grid.Column="1"
               NavigationUIVisibility="Hidden"
               Background="#F5F5F5"/>

    </Grid>
</Window>