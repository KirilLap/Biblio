<Window x:Class="BibAdmin.TransferSessionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пересадить пользователя"
        SizeToContent="Height" Width="420" MinHeight="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <StackPanel Margin="28,24,28,24">

        <TextBlock Text="Пересадить пользователя"
                   Foreground="White" FontSize="16" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,6"/>

        <TextBlock x:Name="TxtSourceInfo"
                   Foreground="#AAAACC" FontSize="12"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap" TextAlignment="Center"
                   Margin="0,0,0,20"/>

        <TextBlock Text="Целевой ПК:"
                   Foreground="#AAAACC" FontSize="12"
                   Margin="0,0,0,6"/>

        <Border BorderBrush="#3D3D6B" BorderThickness="1" CornerRadius="8" Margin="0,0,0,24">
            <ComboBox x:Name="CmbTarget"
                      Background="#2A2A3E" Foreground="White"
                      BorderThickness="0" FontSize="14"
                      Padding="12,8"/>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена"
                    Click="BtnCancel_Click"
                    Background="#2A2A3E" Foreground="#AAAACC"
                    FontSize="13" Padding="24,10"
                    BorderThickness="1" BorderBrush="#3D3D6B"
                    Cursor="Hand" Margin="0,0,8,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Пересадить"
                    Click="BtnConfirm_Click"
                    Background="#3D3D6B" Foreground="White"
                    FontSize="13" Padding="24,10"
                    BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

    </StackPanel>
</Window>
