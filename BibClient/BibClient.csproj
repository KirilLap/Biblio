<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <!-- Post-build: копируем BibClientGuardian.exe и BibClientService.exe в папку вывода -->
  <Target Name="CopyDependenciesAfterBuild" AfterTargets="Build">
    <Copy SourceFiles="$(ProjectDir)..\BibClientGuardian\bin\$(Configuration)\net10.0-windows\BibClientGuardian.exe"
          DestinationFolder="$(OutDir)"
          SkipUnchangedFiles="true"
          ContinueOnError="true" />
    <Copy SourceFiles="$(ProjectDir)..\BibClientService\bin\$(Configuration)\net10.0-windows\win-x64\BibClientService.exe"
          DestinationFolder="$(OutDir)"
          SkipUnchangedFiles="true"
          ContinueOnError="true" />
    <Message Text="✅ BibClientGuardian.exe и BibClientService.exe скопированы в $(OutDir)" Importance="high" />
  </Target>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" Version="10.0.7" />
    <PackageReference Include="System.Drawing.Common" Version="10.0.7" />
  </ItemGroup>

</Project>
